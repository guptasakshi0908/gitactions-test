name: print greeting messages
on: push
env: 
  greet: PM Modi

jobs: 
  greet-everyone:
    runs-on: ubuntu-latest
    env:
      greet: Irani
    steps:
      - name: greet job
        run: echo "$greet"
      - name: greet job another
        run: echo "$greet"
        env:
          greet: Amit Shah
